name: Test my actions

on:
  push:
  pull_request:

jobs:
  example-run:
    runs-on: ubuntu-latest
    name: Example run2
    steps:
      - uses: pavolloffay/github-actions/helloworld-composite/@main
